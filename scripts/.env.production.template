# Stock Tracker Production Environment Configuration
# Copy this file to .env.production and fill in your actual values
# IMPORTANT: Never commit .env.production with real credentials!

# ============================================
# Database Configuration
# ============================================
# MySQL server hostname or IP address
DB_HOST=<MYSQL_HOST>

# MySQL server port (default: 3306)
DB_PORT=3306

# Database name
DB_NAME=stocktracker

# Database username
DB_USERNAME=stocktracker

# Database password - REPLACE WITH SECURE PASSWORD
DB_PASSWORD=<DB_PASSWORD>

# ============================================
# JWT Configuration
# ============================================
# JWT signing secret - MUST be a secure random string
# Generate with: openssl rand -base64 64
# IMPORTANT: Keep this secret and use a different value than the default!
JWT_SECRET=<JWT_SECRET>

# ============================================
# Spring Configuration
# ============================================
# Active Spring profile (should be 'prod' for production)
SPRING_PROFILES_ACTIVE=prod

# Google OAuth 2.0 Configuration
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
OAUTH2_REDIRECT_URI=https://mydomain.example.com/oauth2/redirect

# ============================================
# Server Configuration (optional)
# ============================================
# Server port (default: 8080)
# SERVER_PORT=8080

# ============================================
# Application Configuration (optional)
# ============================================
# Logging level for application (default: INFO)
# LOGGING_LEVEL_STOCKTRACKER=INFO

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 1. Copy this template:
#    cp .env.production.template .env.production
#
# 2. Replace all <PLACEHOLDERS> with actual values:
#    - <MYSQL_HOST>: Your MySQL server IP or hostname (e.g., 192.168.1.50)
#    - <DB_PASSWORD>: Secure database password
#    - <JWT_SECRET>: Generate with 'openssl rand -base64 64'
#
# 3. Protect the file:
#    chmod 600 .env.production
#
# 4. NEVER commit .env.production to version control!
#    (Already in .gitignore as scripts/.env.production)
